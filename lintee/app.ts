console.log('123');

const arr = ['1', '2', '3'];

if (arr instanceof Array) {
    console.log(arr.length);
}

const str: string = 'asdfasfd';

const hidePopup: () => void;

interface IModel {
    name: string;
}

interface ModelBase {
    name: string;
}
